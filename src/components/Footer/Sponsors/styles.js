import styled from 'styled-components';

export const DivSponsors = styled.div``;

export const TextTitle = styled.h2`
  font-size: 22px;
  font-weight: 700;
  line-height: 27px;
  color: var(--white);
  text-align: center;
`;

export const TextContent = styled.p`
  color: var(--white);
  font-weight: 400;
  line-height: 44px;
`;
